language: go

sudo: required

services:
  - docker

go:
  - "1.x"

before_install:
  - docker swarm init
  - docker pull ubuntu:latest

install:
  - curl https://raw.githubusercontent.com/golang/dep/master/install.sh | sh
  - dep ensure
